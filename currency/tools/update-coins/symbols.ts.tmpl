/**
 * @autogenerated
 */

export enum Currency {
    // Euro
    EUR = 1,
    // United States Dollar
    USD = 2,{{range $k, $v := .}}
    // {{$v.Name}}
    {{$v.Symbol}} = {{$v.Num}},{{end}}
}
